*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
#uncomment public/build if building assets on the server with every deploy
#/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper*.php
Homestead.json
Homestead.yaml
Thumbs.db
npm-debug.log
docker-compose.override.yml

# MacOS
.DS_Store
.DS_Store?

# Dusk quirk
:memory:

# uncomment if generating front-end assets on every build
package-lock.json

# in your deployed app you SHOULD commit composer.lock to VCS according to dependencies consistent with your server; so comment-out the following line for your own repo
composer.lock
